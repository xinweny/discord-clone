import type { Participant } from 'livekit-client';

type GroupParticipantLoopProps = {
  participants: Participant[];
};

export function GroupParticipantLoop({ participants }: GroupParticipantLoopProps) {
  return <></>;
}